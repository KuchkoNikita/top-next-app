import { FooterProps } from './Footer.props';
import styles from './Footer.model.css';
import cn from 'classnames';

export const Header = ({  }: FooterProps): JSX.Element => {
	return (
		<>
			
		</>
	);
};